#!/bin/bash
# Liveness probe: Check if the game server process is running
# Used by Docker HEALTHCHECK to determine if container should be restarted
# Exit 0 = healthy, Exit 1 = unhealthy

if pgrep -f "ArkAscendedServer|AsaApiLoader" >/dev/null 2>&1; then
    exit 0
fi

exit 1
